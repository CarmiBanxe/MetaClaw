# Полный review всех ID — единое состояние

**Проверяемое состояние:** влито в `main` = **`016ce72b6224`**, post-merge **37513 `success`**, 466 с,
43 задания успешно + 1 `allow_failure=True`. Гейт на выгрузке `main`: `complete_chains=11 broken_links=11`; влито в `main` = **`e3fb01748`**. Прежняя ссылка на `a304d9f28d6d`, post-merge пайплайн **37369**
(`success`, 483 с, 41 задание успешно + 1 `allow_failure=True`). Требования проверены **на влитом
состоянии**, выгруженном из `origin/main`, а не на рабочей копии. Все результаты ниже относятся к этому SHA; где основание
получено на другом состоянии, это указано в строке.

Полнота: **25** ID спецификации (R1–R9, N1–N5, E1–E5, A1–A6) + **10** установки (L1–L10) +
**5** дефектов (D1–D5) + ограничения и определение готовности. У каждого пункта один статус.

Правило доказательства: описание правила не доказывает исполнение; чистое дерево не доказывает
отсутствия секретов; регистрация команды не доказывает выбор обработчика при вызове.

---

# ВЕРДИКТ I — установка петли

| ID | Статус | Доказательство (SHA `e0ca75622`) |
|---|---|---|
| L1 копии соответствуют шаблону | **PASS** | `copies_found=2/2 section_match=true` |
| L2 команды на обоих уровнях | **PASS** | `commands=8`: `spec`, `build`, `spec-review`, `spec-status` × 2 |
| L3 пути чтения охвачены | **PASS** | `canon-read.sh` печатает блок петли, `exit=0`; `gsd-methodology.md` §«Согласование с петлёй» |
| L4 сессии получили редакцию | **PARTIAL** | эталон `ba3da464…1c302`/16042 б; **4 подтверждения из 11 адресатов**, полный хеш сверен посимвольно; 3 доставки `held`, 7 без ответа |
| L5 проверка отвергает неисправную установку | **PASS** | `_sl_rc=$?` без конвейера; `guard_rc=1` при провале и при отсутствии скрипта; `exit "$guard_rc"` |
| L6 review по каждому ID | **PASS** | настоящая редакция: все **40 ID** на месте (проверено машинной сверкой, пропусков 0), у каждого один статус, таблицы и сводка согласованы построчно, противоречивых утверждений CLEAN нет, состояние привязано к конкретному SHA |
| L7 переходы CLEAN → интеграция | **PASS** | правило у заголовка готовности; готовность разведена на две части |
| L8 slash-команды вызывают режимы | **PASS** | вызовы **всех четырёх** команд найдены в журналах с развёрнутым текстом эталона после подстановки аргумента — подтверждено для версии 2.1.263. Мои прямые пробы дали то же для `/spec-status`, `/spec` и контрольного `/review` (уходит во встроенную проверку кода). Перечень харнесса при `init`: `build, spec, spec-review, spec-status, code-review` |
| L9 интеграция | **PASS** | устройство звена влито: `main=016ce72b6224`, CI ветки 37511 `success` 428 с, post-merge **37513 `success`** 466 с. Проверено на выгрузке из `origin/main` вызовом самого гейта |
| L10 непрерывность проверяема | **PASS** | эталон `governance/spec-loop-commands/`, сверка целиком; 7 проб CENTRAL — все FAIL, контроль PASS; самотест 21/0 |

**Вердикт I (установка петли): NOT CLEAN.** Незакрытые пункты перечислены в единой сводке в
конце документа — промежуточных вердиктов по разделам этот документ больше не содержит.

| Дефект | Статус | Доказательство |
|---|---|---|
| D1 ложный PASS | **PASS** | N1→FAIL, N2→FAIL, N3 вне CI→FAIL, N4 `ci=True`→PASS |
| D2 счёт копий | **PASS** | `copies_found/copies_expected` |
| D3 вычитка охватывает петлю | **PASS** | блок печатается, код учитывается |
| D4 счёт проверок | **PASS** | `checks=21`; `quiet` убрал ложный `GATE_RESULT status=FAIL` |
| D5 ожидание с пределом | **PASS** | предел от медианы, отсчёт от начала исполнения |

---

# ВЕРДИКТ II — спецификация `mcb-11j-map-front`

## R1–R9

| ID | Статус | Доказательство |
|---|---|---|
| R1 агент с точкой входа | **PASS** | три службы объявлены; 69 паспортов |
| R2 семь атрибутов | **PASS** | `OWNER_ROLE_RESOLVED=true`, `APPOINTMENT_STATUS=UNAPPOINTED` |
| R3 владелец не из одного упоминания | **PASS** | связи найдены чтением кода с адресом метода |
| R4 восемь полей у 77 функций | **PASS** | 77 функций, без полного набора — 0 |
| R5 классификация по решению | **PASS** | `EXECUTION_LOCATION`: LOCAL ×77 |
| R6 облачная активация не производится | **PASS** | `cloud_activated=0`; кандидатуры 27/25/25 |
| **R7 цепь замкнута для каждого источника** | **FAIL** | устройство звена перестроено (`157517f90`): `{state, value}`, перечень состояний **свой для каждого звена**. Честный замер: **замкнуто 11 из 21**, разрывов 11 — `source` 10, `claim` 1 (Waku Agent). Требование «для каждого источника» не выполнено |
| R8 подсадка в отдельное дерево, отказ механизма | **PASS** | `tempfile` в 7/7; `subprocess.run([sys.executable, CHECKER, "--grants", <врем. путь>])`; `scope=MECHANISM_REFUSED tree=SEPARATE`; 7 прогонов PASS, коды DIR-DENY-001…007; дерево после — 0 изменённых |
| R9 числа только убывают | **PASS** | `TOTAL=324`, `CLASSIFIED=324`; `still_executable_and_unowned` пуст |

## N1–N5

| ID | Статус | Доказательство |
|---|---|---|
| N1 секретов нет; пробы не на рабочем дереве | **PASS** | Gitleaks **8.18.4**, конфиг проекта `.gitleaks.toml`. Три пробы, все `rc=0`, **находок 0**: (1) всё рабочее дерево `--no-git`; (2) история `origin/main..HEAD`, 5 коммитов; (3) влитые коммиты задания `2c74bd14f..bb9827a2a`. Вторая половина требования: дерево после полного прогона — 0 изменённых. **Ограничения:** в `.pre-commit-config.yaml` заявлена версия **8.30.1**, установлена 8.18.4 — правила новее не применялись; охват — область задания, не весь репозиторий за всю историю; отчёт снят с `--redact`, фрагменты находок не выводились |
| N2 fail-closed | **PASS** | неизвестный класс → `LOCAL_ONLY`; `NOT_ELIGIBLE` по умолчанию; DIR-DENY-коды отвергают неизвестное полномочие |
| N3 `GATE_RESULT` у каждого гейта | **PASS** | проверено на четырёх гейтах |
| N4 гейты не ослабляются | **PASS** | `code-placement`: 1686 = baseline |
| N5 честность величин | **PASS** | замысел, критерий и результат разведены на разные состояния; счётчик `evidence_obtained` показывает **0 из 21** отдельной величиной. Состояние `OBTAINED` требует **адреса** результата — путь, номер прогона, хеш; формулировка критерия отвергается: «значение не несёт адреса результата». Проверено шестью пробами, самотест 22 |

## E1–E5

| ID | Статус | Доказательство |
|---|---|---|
| E1 наследуемый класс → наивысший | **PASS** | правило объявлено и исполнено: 77/77 с полным набором полей |
| E2 вынос данных → `HUMAN_DECISION_REQUIREMENT=true` | **PASS** | коммит `77fb192e1` завёл **явный признак `CROSSES_PERIMETER`**: True у 5 функций из 77 (FN-BI-05, FN-GW-12, FN-GW-15, FN-PL-02, FN-PL-10), у всех пяти `HDR=True`, нарушений 0. **Правило проверяется**, а не только объявлено: `function-classification.py:226`. **Отрицательная проба**: подсадка «вынос=true, решение человека=false» → FAIL с формулировкой «функция ВЫНОСИТ данные за периметр, а решение человека не требуется»; контроль — PASS. Мой прежний поиск регулярным выражением находил лишь 3 из 5 — выборка по словам была недостаточной, понижение до PARTIAL было верным |
| E3 блок не установлен → `LOCAL_ONLY` с причиной | **PASS** | `FAIL_CLOSED_REASON` у всех 77; `EXECUTION_LOCATION=LOCAL` ×77 |
| E4 проба без файла считается несуществующей | **PASS** | 18 исполняемых проб; несуществующие имена устранены |
| E5 источник без authority не даёт claim | **PASS** | правило действует в проверке (`:211`) и в генерации: `"claim": (…) if authority != BREAK else BREAK`. Подтверждено на проверенной редакции |

## A1–A6

| ID | Статус | Доказательство |
|---|---|---|
| A1 `still_executable_and_unowned` = 0 | **PASS** | список пуст; 25 без агента, с исполнимым кодом — 0 |
| A2 `NOT_YET_CLASSIFIED` = 0 из 324 | **PASS** | `functions_total=324`, `by_mode` 296+18+10 |
| A3 ни одной функции CLOUD | **PASS** | 0 совпадений |
| A4 семь проб с отказом механизма | **PASS** | пункт очищен от ослабленной редакции (проверка на прежние формулировки — 0 совпадений); описывает временный каталог, отдельный процесс, отказ по коду + ненулевой код возврата |
| **A5 замкнутые цепи и разрывы** | **FAIL** | следует за R7: 10 из 21 |
| A6 полный набор гейтов — ноль отказов | **PASS** | на `e0ca75622`: прогон CENTRAL 139 команд, 1 отказ — **артефакт извлечения** (сломанная многострочная команда), настоящих 0; дерево после — 0 изменённых. Прогон исполнителя на том же дереве: 143 команды, 0 отказов |

## Ограничения и готовность

| Предмет | Статус | Доказательство |
|---|---|---|
| роли из каталога | **PASS** | новые роли не заводились |
| паспорт с цепью реестров одним коммитом | **PASS** | шард + `IL-SEQUENCE.json` + `INSTRUCTION-LEDGER.md` |
| ADR-060, номер IL заморожен при merge | **PASS** | номер перечеканен 2269 → 2272 при ребейзе, `build_ledger --check` OK |
| готовность: до-интеграционная | **FAIL** | требует PASS по каждому A; A5 — FAIL |
| готовность: интеграционная | **PASS для `4206f7366a1c`** | исторический факт: post-merge 37387 `success`, 421 с. Относится к проверенному SHA и на последующие исправления не переносится |

**Вердикт II (спецификация): NOT CLEAN.** Незакрытые пункты — в единой сводке в конце документа.

---

# §L8 — что именно не проверено

Коллизия имён снята: `review`/`status` удалены, заведены `spec-review`/`spec-status`, харнесс
зарегистрировал их как навыки в живом окне CENTRAL. **Но регистрация не есть выбор обработчика
при вводе.** Изнутри сессии текст в собственное приглашение не подаётся, а вызов Skill адресует
обработчик напрямую и коллизию обходит.

**Проверяемый путь, не создающий конкурирующей линии:** запуск отдельного экземпляра Claude
Code в неинтерактивном режиме с подачей строки команды на вход и чтением того, какой обработчик
отработал. Это внешний тест, не пишущий в дерево. **Выполнимость не установлена** — требуется
проба.

---

# Сводка незакрытого

Состояние: влито в `main` **`016ce72b6224`**, post-merge **37513 `success`**.

| ID | Статус | Что требуется |
|---|---|---|
| **R7** | **FAIL** | замкнуто **11 из 21**; разрывов 11 — `source` 10, `claim` 1 (Waku Agent). Требование «для каждого источника» не выполнено |
| **A5** | **FAIL** | следует за R7 |
| готовность: до-интеграционная | **FAIL** | требует PASS по каждому A; A5 — FAIL |
| **L4** | **PARTIAL** | 4 подтверждения из 11; состояние остальных **неизвестно** до положительного события |

**Итого не закрыто: 4 пункта** — 3 FAIL, 1 PARTIAL.
Все три FAIL сводятся к одному предмету: **цепи замкнуты у 11 источников из 21**.

## Единый вердикт

**Спецификация: NOT CLEAN** — R7 и A5.
**Установка петли: NOT CLEAN** — L4.

Устройство меры влито и проверено: состояние звена отделено от текста, перечень состояний свой
для каждого звена, семь структурных обходов из восьми отвергнуты, восьмой закрыт требованием
адреса результата. Числа стали честными — прежнее «21 из 21» было ложным.

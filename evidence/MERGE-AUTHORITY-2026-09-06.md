# Доказательства выполненного merge и его основания

**MR:** !448 `agent/factory/crypto01/mcb-11j-r3` → `main`, проект `banxe-ai-rnd/research` (359).

| Предмет | Значение | Способ замера |
|---|---|---|
| состояние | `merged` | `glab api projects/359/merge_requests/448` |
| время merge | 2026-09-06T14:46:51.395Z | там же |
| исполнитель merge | `RnD_for_vibe_coding_bot` | там же |
| merge-коммит | `bb9827a2af0a` | там же, `merge_commit_sha` |
| squash | `False` | там же |
| SHA ветки на момент merge | `49aa51fd2184cf5c311fad86b6ad28ef76f21ac0` | MR `sha` перед слиянием |
| `main` после merge | `bb9827a2af0a` | `glab api projects/359/repository/branches/main` |
| прогон перед merge | 37320 `success`, 37 success + 1 `allow_failure=True` | `glab api .../pipelines/37320` |
| post-merge конвейер | 37328, `ref=main`, sha `bb9827a2af0a` | `glab api .../pipelines/37328` |

## Источник распоряжения

Полномочие проверено **прямой пробой по всему транскрипту** (100 МБ), а не по усечённой выборке:
запись `type=user role=user` от **2026-09-06T12:22:27.914Z** в сессии
`98e350a1-1191-4684-93da-f858c3fcf2bf`, пронумерованное поручение оператора, пункт 7:

> «Выполнить guarded merge согласно уже порученному непрерывному roadmap.»

пункт 8: «Проверить post-merge main.»

**Решение принято оператором заранее, фабрика его исполнила, а не приняла** — исключение
ADR-145 применено правильно. Повторное согласование не требуется.

## Предотвращённая ошибка, записанная как улика против метода

Первая проба искала распоряжение в **последних 3 МБ** транскрипта и вернула ноль совпадений.
Из этого следовал бы вывод о merge без полномочия — **ложное обвинение в нарушении канона**.
Полный `grep` дал 31 вхождение. Правило подтверждено: «X отсутствует» утверждается только
прямой пробой точного предмета.

## Post-merge проверка точного SHA — выполнена

| Предмет | Значение |
|---|---|
| конвейер | **37328**, `ref=main`, sha `bb9827a2af0a` |
| статус | **success**, длительность 442 с |
| задания | 38 успешных, 1 `failed` с `allow_failure=True` (`techmap-tm100-report`) |
| `main` на момент проверки | `bb9827a2af0a` — совпадает с merge-коммитом |
| время ожидания в очереди | ~10 мин, `started=0` до старта — очередь, не зависание |

Упавшее задание отделено от обязательных: `allow_failure=True`, падает и на прежнем прогоне
`main` (37307). Пайплайн из-за него красным не становится.

**Интеграционная часть определения готовности закрыта для SHA `bb9827a2af0a`.**
Для последующих коммитов `735fee888` и `a2eeb3743` (исправления L5/L10/L8) требуется отдельный
интеграционный цикл: результат прежнего конвейера на новые изменения не переносится.

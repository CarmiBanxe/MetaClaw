# Проверка сведения `30fbec2f`: мера сквозных сценариев и шестнадцать якорей

**Замер:** 2026-09-11, `~/wt/fx-integration`, вершина **`30fbec2f`**, дерево чисто.

## Вердикт назван вместе со средой — в обеих

| среда | `dist` | КОД | адреса | якоря | вердикт |
|---|---|---|---|---|---|
| собранное дерево | есть | **0** | 83 — **все 83 разрешаются** | 48, не оправдано **0** | **PASS** |
| чистый клон | **0 каталогов** | **1** | 83 — разрешаются 80, **НЕ РАЗРЕШАЮТСЯ 3** | 48, не оправдано **0** | **FAIL** |

Существенное: **48 якорей держатся и без сборки**. Три неразрешившихся — те самые адреса внутрь
`dist`, названные оператору; ни одного иного.

Упоминаний адреса без якоря: **51 → 35**, убыло **ровно на 16** — на число объявленных якорей.
Общее число адресов **83 не изменилось**: датированные приписки «ЧТО БЫЛО» называют прежние числа
**без обратных кавычек**, иначе они сами стали бы адресами, протухли и раздули счёт. Мелочь, которая
решает: **форма цитаты определяет, станет ли история новым долгом**.

## Линия поправила МЕНЯ, и поправка была по существу

Я выдала перечень съехавших адресов с оговоркой «мною не проверялся». Оговорка спасла:

| я послала | что там на самом деле | верное место |
|---|---|---|
| `main.ts:322` | чужой комментарий про `LOCAL_ENFORCEMENT_NOT_COMPOSED` | **`:329`** — `composeLocalEnforcement("qwen2.5")` |
| `client-enforcement.ts:116` — без нового числа | `enforceForDoor(…)` — родственное, но другое место | **`:83`** — `export type ClientTact` |

Проверено мною построчно: обе поправки верны.

**Мой `main.ts:322` разрешился бы и указал мимо** — ровно та порча, ради которой якорь и заведён.
**Инструмент, который я заказала, поймал на ней меня же**, и это лучшее свидетельство, что он
работает: сторож, ловящий своего заказчика, не подогнан под него.

Сверх моего перечня линия нашла два съехавших адреса (`cloud-broker.ts:129`,
`write-once-durable.ts:17`) и **семь адресов проверила и оставила верными**, числа не тронув.
Отдельно: два адреса **без номера строки** якоря не получили — «якорь есть утверждение о строке,
а строки там не названо».

## Что не сдвинулось

Пройденность **5 / 2** на всех четырёх прогонах. Три `dist`-адреса не тронуты — предмет оператора.
Проза заметок не сокращена; правлены числа и приписан датированный абзац. Самотест: 16 подсадок +
7 проб на итог и якорь, расхождений 0 **в обеих средах**.

## Вердикт

**Сведение принято.** Мера, которая утром печатала «все разрешаются» литералом и не видела неверного
адреса, к вечеру выводит слова из величин и ловит сдвиг верного адреса на одну строку.

# Восемь решений, закреплённых за оператором — сведено в одно место

**Составлено:** 2026-09-11, вечер. **Основание фактов:** замеры этого дня на деревьях `b87a9041`,
`75aa552a` и ветвях линий; каждое число снято прогоном и названо вместе со средой.

> **Почему обращение одно, а не семь.** Пункты связаны числом: **225 функций из 324** держит
> **один** из них, и пока он открыт, остальные шесть меняют карту на единицы. Разделив их, я бы
> предложила вам семь равных решений — а они не равны.

---

## 1 · УСЛОВИЕ 2 — названный человек-выпускающий ⟵ **держит 225 функций**

**Состояние:** выпущено **ноль**. Разряды: карантин **94** · подстановка песочницы **5** · выпуск
человеком **0**.

**Что проверено и снимает прежние опасения:**

- донорские наборы проб существуют для **225 из 225** — прежнее «девять» было ошибкой поиска
  (искала донорские *тесты* там, где лежит донорский *код*);
- условия 4 и 5 показаны проходимыми; образец пакета свидетельств лежит в дереве;
- **подстановка не притворяется человеком**: имя намеренно непроизносимо (`stub:<роль>@sandbox`), и
  она стоит за **лицо**, никогда — за **разделение**;
- устройство **fail-closed в верную сторону**: любое отклонение `policy/mode.yaml` возвращает весь
  эмулированный набор в карантин **арифметикой**, без чьей-либо памяти.

**Что остаётся вашим:** назвать человека. **Инженерной работой это не заменяется** — ни одной.

---

## 2 · ОТЗЫВ УЧЁТНОГО ТОКЕНА на `gitlab.banxe.sits.pro` ⟵ **единственный срочный**

**Факт:** **25 отслеживаемых** `package.json` несут **92 буквальных значения** вида `user:token@`.
Имена — `deploy-libs`, `banxe-core`, `banxe-common`. **Значений я не печатала нигде** — проверена
только форма.

**Тревога не ложная и не новая:** найдено 2026-08-27, и секрет-скан дерева заведён **этим самым
случаем** («двенадцать заданий CI и **ни одного секрет-скана**»). Сегодня: `imports/` — храповик,
**все 25 в реестре, вне его ноль**, `secret-scan: PASS`.

**Но дерево дважды говорит о себе то, чего замер закрыть не может:**

> «Удаление из дерева и даже из истории **НЕ отменяет** того, что значение покинуло границу.
> Единственное лечение — **отзыв на стороне выдавшей системы**.»

Узел живой, и он же есть `origin` дерева (самоуправляемый GitLab, **не** публичный GitHub).
**Произошёл ли отзыв — не знает ни один прибор.**

---

## 3 · ADR по I-04 — пороги `£10 000` и `£50 000`

Четыре решения, разобранные отдельным документом: классификация двух постоянных книги · связь
флага и удержания · вправе ли флаг одобрения оставаться двоичным · применимость к фиатной книге.

**Замер снял главную посылку:** собственный реестр порогов дерева (**6 величин / 30 домов /
12 исключений**) существует, и я его прежде не читала. **Двухшаговое устройство нормой не
объявлено** — оно выглядит разумным, и этого недостаточно.

---

## 4 · РАЗВИЛКА ПАМЯТИ — набор предложенного как `proposal_` или `trace_`

`proposal_` стирается забыванием — но тогда рушатся **собственные незавершённые вызовы клиента**.
`trace_` переживает забывание — но перечень предложенного **остаётся в банке**.

**Обе ветви защитимы, и выбор есть решение о защите данных, а не о коде.**

---

## 5 · КАРТА KYC — что есть «фактическая интеграция» восьми функций

Контракт имеет **шесть равных реализаций и ноль потребителей**. Ваше прежнее слово:
«**восемь функций закрывать по фактической интеграции и проверкам, а не по появлению библиотеки**».
Три возможности названы, ни одна не выбрана; **все три суть решения о карте**.

---

## 6 · X-05 — фактическое назначение держателя рычага

Механизм построен и проверен: **назначение · отзыв · аудит**. Основание требуется и для остановки,
**и для освобождения**. Остаётся само назначение — условие производственного запуска, **песочницу
не задерживающее**.

---

## 7 · СЛОВАРИ ДВУХ СТОРОН — `APPROVE/REJECT/ESCALATE` против `PROCEED/RESTRICT/NO_DECISION_YET`

Соответствие записано таблицей и названо **находкой замера, а не нормой**. Согласование — ваше.

---

## 8 · УТОЧНЕНИЯ КАРТЫ, КОТОРЫХ В КАРТЕ НЕТ ⟵ **закрывает пять функций для объявления**

Добавлено вечером 2026-09-11 — **это не решение о продукте, а правка карты**, и потому короткое.

**Пять функций карты числят за собой «уточнения», которых в самой карте не существует.** Проверено
прямым перебором множества `function_id` по принятому манифесту:

```
FN-AU-03  →  FN-AU-03/u1 · FN-AU-03/u2
FN-AU-08  →  FN-AU-08/u1 · FN-AU-08/u2
FN-PM-15  →  FN-PM-15/u1 · FN-PM-15/u2 · FN-PM-15/u3
FN-RC-05  →  FN-RC-05/u1 · FN-RC-05/u2
FN-RC-09  →  FN-RC-09/u1 · FN-RC-09/u2
                                   одиннадцать имён, ни одно не разрешается
```

**Мера отвергает объявление устройством**, и верно: функция, чьи уточнения не определены, не
описана целиком, а «половина функции функцией не является» — правило реестра, уже применённое к
другим.

**Чем это стоит вам прямо сейчас.** `FN-AU-03` «восстановить доступ» **построена и проверена**:
носитель есть, четыре якоря дают целое совпадение, набор — 372 пробы, код 0, граница хранения
выражена значением и закреплена пробой. **Она не может быть объявлена покрытой только из-за этого.**

**Чего Фабрика делать не вправе:** дописать определения. Это значило бы **решить за владельца карты,
что уточнения означают** — то есть задним числом назначить предмет функции.

**Что требуется:** назвать предмет одиннадцати уточнений **либо снять их из манифеста**. Любое из
двух — акт владельца карты, и оба разблокируют разом до **пяти** функций.

### 8б · частный случай, появившийся в тот же вечер: запись карты РАЗОШЛАСЬ с деревом

`FN-LG-02` «вести план счетов» числится `CARRIER_INCOMPLETE` с записью:

> «род счёта задан перечислением из пяти значений; **ПЛАНА СЧЕТОВ как ведомого реестра с иерархией,
> открытием и выводом из употребления НЕТ**»

**Носитель с этим механизмом в дереве появился** — проверено: `chart-of-accounts.ts`, 224 пробы,
иерархия без возможности цикла **по построению**, вывод из употребления без удаления, допуски в
отдельном пространстве.

**И у той же записи вторая неточность, обнаруженная замером:** пять значений `AccountType` суть
**бухгалтерская классификация проводки**, а не **род счёта**. Показано находкой в самом дереве, где
**обе оси живут одновременно** и связаны явной таблицей: «система, у которой оси совпадали бы, такой
таблицы не держала бы».

**Два разных дела, и смешивать их нельзя:**

- **текст записи стал ложен** — правка текста есть **акт замера**, и Фабрика её сделает;
- **закрывается ли функция «ВЕСТИ» без утверждённого СОДЕРЖАНИЯ плана счетов** — **вопрос к вам**.
  Механизм ведения построен; **содержание есть решение учётной политики**, и единственный кандидат в
  дереве (донорский перечень из карантина, плоский, без кодов счетов, размеченный третьей осью)
  **не принят намеренно**: «принять его значило бы принять учётную политику».
---

# Что сделано без вас и чем это ограничено

**Покрытие сдвинулось впервые: `47/324` → `48/324`** (проверено моим прогоном и моей арифметикой:
`48 + 276 = 324`, пересечение пусто). **Очередь исполнимого — семь функций**, и число трижды
уменьшалось **от чтения**: `21 → 14 → 7`. Ожидание по именам разрядов было завышено **втрое**.

> **Семь функций есть `47 → 54` в лучшем случае.** Одна шестая карты. **Двести двадцать пять
> держит пункт 1.**

И оговорка, которую линия поставила сама и которую я повторяю: **живой внешней двери у закрытой
функции нет**. `48/324` означает «носитель с якорем и набором проб», **а не «работает в
производстве»**.

# Чего я НЕ прошу

Ни одного технического разрешения. Всё, что можно измерить, починить и проверить без вас, идёт
непрерывно: за этот вечер — сведение восьми ветвей, четыре новых правила в оснастке, шесть моих
собственных ошибок названы и закрыты приборами, а не памятью.

---

# ПОПРАВКИ, снятые вечером 2026-09-11 — два пункта из восьми описаны мною точнее, чем измерено

Прежний текст **не стёрт**: ниже сказано, что в нём неверно и чем заменяется. Стирать значило бы
лишить вас возможности увидеть, **как** ошибка выглядела.

## Поправка к пункту 1 — «держит 225 функций»

**Число верно. Обещание, которое из него читается, — нет.**

`blocking-reason-gate.py` на вершине `30fbec2f` печатает, помимо корзин:

```
второе основание: пакет называет у 4 — в реестре заполнено у 4
  FN-BI-01, FN-BI-02, FN-BI-04, FN-PM-01:  PARITY_QUARANTINE_UNRELEASED → CARRIER_INCOMPLETE
```

**У четырёх функций за карантином стоит ВТОРОЕ препятствие**, и снятие Условия 2 их не покроет:
носитель неполон, и он останется неполон.

**Насколько это распространено — не измерено.** Пакет доказательств паритета высказывается о
**восьми** функциях; из этих восьми второе основание нашлось у **четырёх**. Об остальных **221**
пакет молчит, и мера этого от них не требует — пустое поле здесь честное «не смотрели», а не дефект.

**Чем это меняет ваше решение.** «Освободите — и станет 225» я утверждать не вправе: **225 есть
верхняя граница**, а единственная имеющаяся выборка дала **половину** со вторым препятствием.
Решение об Условии 2 от этого не отменяется — оно по-прежнему держит больше всего, — но **ожидаемая
отдача названа честно: неизвестна и, вероятно, заметно меньше 225**.

**Оценка уже делается.** Прежде я написала здесь, что «могу поручить замер» и не начинала его,
«чтобы не подменить ваш вопрос своим». Это было неверно: **замер не подменяет решение, он делает его
осмысленным**, и придерживать читающую работу ради чистоты формулировки значит оставить вас решать
на худших данных. Выборочный замер запущен: **двадцать функций из 225**, отбор псевдослучайный с
объявленным зерном и перечнем, напечатанным **до** чтения, четыре уже измеренные — исключены.
Исход по каждой: носителя нет · закрыт частично · закрыт целиком · не определено. Результат ляжет
сюда же с оговоркой о точности.

## Поправка к пункту 2 — числа и, важнее, состояние охраны

**Числа были сняты прежним, более узким прибором.** Переснято одной мерой на двух коммитах
(`b87a9041` — 96 коммитов назад — и вершине сведения):

```
файлов 30, вхождений формы «имя:секрет@» 102  (97 литеральных + 5 шаблонных), вне imports/ 0
```

На обоих коммитах **одинаково**. Прежнее «25 файлов, 92 значения» было **не другим состоянием,
а другим прибором**: мой образец недосчитал пять файлов. **Роста нет**, всё внутри объявленного
карантина `imports/`, **значения не печатались нигде** — считалась только форма.

**Новое и более важное, чего в прежнем тексте не было вовсе.**

2026-09-08 вашей директивой сняты **две** работы конвейера: `secret-scan` и `credential-containment`.
2026-09-09 гейт с именем «средство контроля, исполняемое нигде, роняет сборку» **вернул одну из
них** — `credential-containment` жива и зовётся сегодня. `secret-scan` **не вернулась**.

Почему вернулась ровно одна: тот гейт смотрит **в один каталог**, `scripts/`. В каталоге `tools/`
лежит **ровно один** файл `.py`, и это `secret-scan.py`. **Единственное несвязанное средство контроля
оказалось в единственном каталоге, куда не смотрит гейт, стерегущий несвязанность.** Заметка же о
снятии в конвейере до сих пор говорит «обе сняты» — и это ложь о живой работе.

**Что я поручила:** сделать снятое **видимым** — привести заметку к сегодняшнему дню, расширить поле
зрения гейта и разрешить возникшую красноту **объявленным поимённым изъятием** с датой, причиной и
именем решившего.

**Чего я НЕ поручала и не поручу:** возврата `secret-scan` в конвейер. Снятие — **ваша директива**,
и возврат есть её отмена. Это ваш акт.

**Что это меняет для срочности пункта.** Отзыв токена в выдавшей системе остаётся **единственным
лекарством**, и здесь ничего не изменилось. Но добавилось второе, отдельное: **машина, которая
поймала бы НОВОЕ учётное данное, входящее в дерево, сейчас отключена и до сегодняшнего дня не была
видна ни одному гейту.** После порученного хода её состояние будет печататься на каждой сборке.

## Поправка к пункту 8 — «самый дешёвый» оказался ещё и измеримо готовым

Я называла восьмой пункт дешёвым: «одно слово разблокирует до пяти функций». Это было верно
и **слабее, чем есть на самом деле**.

**Одиннадцать уточнений существуют идентификаторами и только ими.** Замер: каждый встречается в
карте **ровно один раз** — в своём же поле `refinements`, и нигде больше; следа вне карты нет.

```
FN-AU-03  восстановить доступ (recovery)               → /u1, /u2
FN-AU-08  управлять ролями/полномочиями (RBAC)         → /u1, /u2
FN-PM-15  обработать возврат/reject/recall/chargeback  → /u1, /u2, /u3
FN-RC-05  классифицировать/приоритизировать break      → /u1, /u2
FN-RC-09  сгенерировать shortfall-alert + эскалация    → /u1, /u2
```

**И вот новое.** `FN-AU-03` **готова**, и это проверено мною **прямой пробой дерева**, а не по прозе
реестра:

```
services/identity/auth/src/access-recovery.ts — есть
AccessRecoveryService · RECOVERY_SCOPE · ACCESS_RECOVERY_PORT · EVENT_ACCESS_RECOVERY_REFUSED — все на месте
граница названа ЗНАЧЕНИЕМ:  RECOVERY_SCOPE = "second-factor-only"
```

Запись состояния движка добавляет то, чего я сама не снимала: набор `auth` прогнан живьём — **372
пробы, 17 наборов, КОД=0**, — и подстановка готовой записи в меру покрытия даёт **ровно одно**
нарушение, и оно **про уточнения**.

**То есть функция не «почти готова». Она готова, и держат её два идентификатора, которых карта не
определяет.**

**У вас два пути, и оба законны.** Внести одиннадцать записей — карта станет подробнее. Убрать
одиннадцать идентификаторов — функции станут объявляемыми **тоже**, дешевле, но при этом теряется
сведение о том, что предмет делится на части, и следующий объявит функцию целой, **не зная о
частях**. Если же части выдуманы — их удаление есть очистка, а не потеря.

**Чтобы вам не решать вслепую, работа уже идёт.** Линия читает дерево и готовит **по каждому из
одиннадцати** предложенную запись **с адресом-опорой**, помечая слабыми те, у которых опоры в дереве
нет; по каждой из пяти функций — довод, какой путь верен, и **что даст разрешение**: станет ли
функция объявляемой сразу или за ней стоит второе препятствие. Результат ляжет **отдельным
документом, не в карте**: карта ратифицирована, и вносить в неё — ваш акт, а не наш.

## Пункт 1 — теперь с числом: **снятие карантина освободило бы около половины, а не всё**

Обещанный выше замер сделан. Двадцать функций из 221, отбор псевдослучайный, **зерно объявлено,
перечень напечатан до чтения**, ни одна не заменена.

```
предмет закрыт целиком     11      ← снятие карантина освободило бы
закрыт частично             9      ← за ними стоит ВТОРОЕ препятствие
носителя нет                0
не определено               0
```

**55 %.** Доверительный промежуток 95 %: **34–74 %**, то есть **от 77 до 167** функций из 225,
точка около **124**. Второе препятствие — примерно у **103**.

**Как это читать.** Полоса делает ровно две вещи: **опровергает «225 станут покрываемы»** и столь же
твёрдо **опровергает «почти ни одна»**. Различить «половину» и «две трети» она не может — и выдавать
55 % за точное число я не стану.

**Что это меняет для решения.** Условие 2 остаётся **самым крупным рычагом из всех восьми**: даже по
нижней границе это **77 функций** — больше, чем даёт любой другой пункт, и больше нынешнего покрытия
целиком. Меняется не выбор, а **ожидание**: за снятием последует не «готово», а вторая волна работы
примерно того же размера.

**И одно наблюдение, которое важнее доли.** Однородность службы оказалась мифом: две функции одного
файла вышли готовыми, тогда как три соседние в том же контуре — известные частичные. **Судьба функции
определяется не службой, а тем, объявлен ли её предмет или исполнен.** Поэтому рассуждение «служба
выпущена — значит, её функции свободны» не работает, и полный ответ даст только сплошной замер.

Подробности, включая **девять расхождений реестра с деревом**, найденных попутно, — в
`MEASURE-CONDITION-2-YIELD-SAMPLE-2026-09-11.md`. Два из девяти стоит назвать здесь, потому что они
про поведение, а не про учёт: **ограничитель частоты обращений отвечает «разрешено» всегда** (заглушка
объявлена, но запись реестра утверждает обратное), и **отказ записи в аудит проглатывается**, отчего
событие может уйти в шину, не оставив следа.

## Пункт 8 — измерен, и обе мои формулировки оказались неверны

Я писала: «самый дешёвый пункт; **одно слово разблокирует до пяти функций**» и «уточнения существуют
идентификаторами, определений нет». **Измерено — неверно и то, и другое.**

### Определения существуют, и они двойной рецензии

Все **одиннадцать** уточнений определены — в том самом входе, из которого карта порождается:

```
banxe-architecture/governance/techmap/semantic-review-records.json    sha256 362b3023…    330 записей
```

**Проверено мною прямой пробой:** одиннадцать из одиннадцати на месте. Каждая запись несёт
наблюдаемое обязательство, контрпример, донорскую точку входа, вердикт, состояние носителя, разряд
отказа — и **двух рецензентов**, основного и независимого.

Пример: `FN-AU-03/u1` — «система позволяет войти запасным кодом при утрате второго фактора».

**Это не выдумка и не черновик. Это отрецензированная людьми работа, которая до карты не доехала.**

### Причина — один шаг порождения, и он чинится Фабрикой

`banxe-architecture/scripts/build-canonical-function-ssot.py`, строки 121–124 берут из записи
рецензии **только идентификатор** и кладут его в поле родителя; цикл построения идёт по объявлениям
в срезах карты, где уточнения не объявлены, — и записи не возникает **никогда**.

**Определения не терялись — они не переносились.** Разница существенна: терять — случайность,
не переносить — устройство. **Ваш акт здесь не нужен: это дефект сценария, и починка уже поручена.**

### Выход измерен, и он не пять

Подстановка готовых записей в меру, дважды на функцию:

| функция | сейчас | с записями | что остаётся |
|---|---|---|---|
| FN-AU-03 | 1 нарушение | **1** | `u2` — рукопожатие только песочное |
| FN-AU-08 | 1 | **1** | `u2` — изменения ролей в дереве нет |
| FN-PM-15 | 1 | **1** | `u3` — отзыва платежа нет |
| FN-RC-05 | 1 | **1** | `u2` — приоритизации нет |
| FN-RC-09 | 1 | **0** | по этой мере — ничего; держит карантин |

**Четырём разрешение открывает не дверь, а вид на второе препятствие.** Пятая становится
объявляемой по мере — и упирается в Условие 2.

**И цена названа числом:** знаменатель **324 → 335**. Мера немедленно скажет «функций вне обоих
перечней: 11», и каждая из одиннадцати потребует **собственного замера** — корзиной «не смотрели»
их не закрыть, храповик это запрещает. Замеры для всех одиннадцати, впрочем, **уже есть** в записях
рецензии.

### Что это меняет для вашего решения

**Вопрос «вносить или убирать» остаётся вашим, но он стал дешевле и честнее.**

**Убирать нельзя дёшево.** Пять из одиннадцати несут **отрицательный** вердикт: изъятие сделало бы
пять функций объявляемыми, **спрятав ровно то, чего им недостаёт**. Довод «если части выдуманы —
это очистка» здесь не действует **ни для одной**: части не выдуманы, они отрецензированы.

Сильнейший случай — `FN-AU-08`: главный глагол её собственного имени, «**управлять**», в дереве не
выражен. Изъятие сделало бы проверку прав читаемой как управление ролями — **не потеря сведения,
а введение в заблуждение**.

**Два вопроса, которых не было в моём прежнем тексте.** У `FN-PM-15` имя называет **четыре** предмета
(`возврат/reject/recall/chargeback`), а уточнений **три**: у `reject` уточнения нет вовсе, и
изобретать его никто не вправе — источник его не создавал. У `FN-RC-09` имя называет **эскалацию**,
о которой **ни одно** из двух её уточнений не говорит.

Готовый текст всех одиннадцати предложенных записей — в дереве, документом-обращением, **не в карте**:
`docs/governance/BRIEF-MAP-REFINEMENTS-ELEVEN-2026-09-11.md`.

## Пункт 5 — посылка устарела: сведение состоялось, потребители есть

Я писала: «контракт имеет **шесть равных реализаций и ноль потребителей**». **Переснято сегодня —
неверно.**

```
дом словаря:  libraries/banksy-kyc-port   ← один
ввозят:       services/onboarding/kyc · services/compliance/compliance · libraries/banksy-seam-check
```

Дубликат снят, и снятие **стережётся**. Проба шва `kyc-contract-seam.spec.ts` объясняет, почему
именно так:

> «стережёт **ТОЖДЕСТВОМ объекта**, а не равенством значений: заново объявленное перечисление с теми
> же буквами равенство значений прошло бы и было бы вторым домом».

Второе объявление в своё время **само назвало условие своего снятия** — «must be re-synced if a path
alias for it is ever added»; псевдоним заведён, дубликат снят.

**Что это меняет.** Вопрос «что есть фактическая интеграция» больше не стоит на посылке «потребителей
ноль» — потребителей **два**. Но ваше прежнее слово от этого только укрепляется: «восемь функций
закрывать по фактической интеграции и проверкам, **а не по появлению библиотеки**». Библиотека
появилась, и **сама по себе она восьми функций не закрыла** — они по-прежнему держатся тем, что их
спецификации лежат вне этого дерева.

**То есть выбирать между тремя возможностями, вероятно, уже не нужно:** ответ следует из вашего же
критерия. Если согласны — скажите одно слово, и пункт закрывается без решения.

---

# Что из восьми пунктов на деле ваше — пересмотрено по одному правилу

Сегодняшний ход дал правило, которое я применила к собственному брифу: **эскалация есть действие и
требует аудита, как всякое другое**. Положив вопрос вам, я совершаю ход — и обязана прежде проверить,
**мой ли это вопрос**.

| пункт | остаётся вашим? |
|---|---|
| **1 · Условие 2** | **да** — освобождающий есть человек, и это назначение |
| **2 · отзыв токена** | **да** — отзыв в выдавшей системе; и возврат снятой проверки, ибо снятие ваше |
| **3 · ADR по I-04** | **да** — пороги закреплены за вами |
| **4 · память `proposal_`/`trace_`** | **да** — выбор о защите данных; но я могу **сузить** его замером: что именно рушится в каждой ветви и сколько его |
| **5 · карта KYC** | **вероятно, нет** — посылка устарела, ответ следует из вашего же критерия |
| **6 · X-05** | **да** — назначение человека |
| **7 · словари двух сторон** | **да в части ратификации** — но таблицу соответствия с обеими читками и ценой каждой напишу я, чтобы вы **ратифицировали, а не проектировали** |
| **8 · одиннадцать уточнений** | **НЕТ** — дефект порождающего сценария, чинится Фабрикой; поручено |

**Два пункта из восьми не были вашими.** Восьмой пролежал днями как операторский, будучи дефектом
одного шага сценария. Пятый стоял на посылке, переставшей быть верной.

Оплачены эти дни не вами, а **работой, которая не делалась**.

## Пункт 4 — сужен замером: выбор не из двух, и третий путь стоит три строки

Я ставила развилку двоичной: `proposal_` **или** `trace_`. **Замер показывает, что она ложная.**

### Как устроено забывание — прочитано в носителе

`libraries/banksy-memory/src/store.ts`, `forget()`:

```
kind "conversation"  →  СТИРАЕТСЯ
kind "trace"         →  остаётся: «append-only, хранится срок хранения (I-24)»
kind "mandate"       →  остаётся: «несёт банковский срок хранения (I-08)»
```

Род **выводится из приставки ключа**, а не передаётся вызывающим — и это сделано намеренно:
«переданный, он однажды разойдётся с содержимым — и разойдётся **в пользу того, кому удобнее
стереть**».

Приставки `proposal_` в перечне **нет**, поэтому сегодня такой ключ падает в `conversation`
и стирается. Отсюда и обе ветви моей развилки.

### Цена третьего рода — посчитана, а не прикинута

```
объявлен  MemoryKind          →  одно место   (keys.ts:29)
выводится kindOf              →  одно место   (keys.ts:35)
решает судьбу при забвении    →  одно место   (store.ts, forget)
отбор по роду при вспоминании →  одно место   (store.ts, recall)
вне библиотеки памяти по роду не ветвится НИЧТО
```

**Четыре места, одна библиотека, ни одной службы.** Добавление рода — один член перечисления, одна
строка приставки, одна ветвь в забвении.

### Что это меняет для вашего решения

Вам предлагалось выбрать между «стереть предложенное целиком» и «сохранить целиком». **Третий род
со своим собственным правилом стоит три строки**, и правило это может быть мягче обоих — например,
**сохранять факт, что предложение было, и стирать его содержание**: тогда незавершённые вызовы
клиента имеют, на что ссылаться, а текст предложения в банке не остаётся.

**Предел моего замера назову честно:** я посчитала цену **добавления рода**. Поддерживает ли
хранилище **частичное** стирание — содержания при сохранении строки — я **не мерила**, и обещать,
что правило «факт без содержания» исполнимо дёшево, не стану.

**Решение остаётся вашим — это защита данных.** Но выбирать теперь можно из **трёх** путей, а не
из двух, и знать при этом, что третий не дорог.

## Пункт 7 — таблица написана, и она сложнее, чем я её объявила

Я писала: «`APPROVE/REJECT/ESCALATE` против `PROCEED/RESTRICT/NO_DECISION_YET`, соответствие
записано таблицей». **Словари не равночисленны.** Сторона надзора несёт **три** исхода, контракт
границы — **шесть**:

```
DECISION_STATUSES = PROCEED · PROCEED_WITH_CONDITIONS · HOLD ·
                    RESTRICT · MORE_INFORMATION_REQUIRED · NO_DECISION_YET
```

### Что достижимо — замер по каждому значению

| исход контракта | производится? | где |
|---|---|---|
| `PROCEED` | **да** | `conductor/src/http/server.ts:1026` |
| `RESTRICT` | **да** | там же |
| `NO_DECISION_YET` | **да** | там же |
| `PROCEED_WITH_CONDITIONS` | **нет** — **ноль** вхождений вне собственного объявления | — |
| `MORE_INFORMATION_REQUIRED` | **нет** — вне объявления только проба | — |
| `HOLD` | **нет** как исход решения; все 14 вхождений — чужие `HOLD` (размещение казначейства) | — |

**Три исхода из шести недостижимы**, и все три достижимых производятся **одной строкой в одной
двери**. Дверь о себе честна:

> «СЛОВАРИ ДВУХ СТОРОН РАЗНЫЕ, И ЭТО НАЗВАНО, А НЕ СГЛАЖЕНО… молчаливое приведение одного словаря
> к другому и есть тот род подмены, ради которого контракт заведён. Расхождение словарей — находка
> замера, а не решение этой правки; сведение их к одному есть **предмет ратификации**.»

### Смысловая потеря, которую видно только рядом

`ESCALATE → NO_DECISION_YET`. Но **эскалация есть решение** — надзорный решил передать выше;
`NO_DECISION_YET` говорит, что решения **нет**. Одно приводится к другому, и **разница исчезает
молча**: получатель не отличит «передано выше» от «ещё не смотрели».

Три недостижимых исхода — ровно те, которыми надзорный **не может** выразиться: «разрешаю с
условиями», «нужны сведения», «придержать». Сегодня для всего этого есть единственный выход —
`ESCALATE`.

### Три пути, и цена каждого

1. **Сузить контракт до трёх.** Дёшево в коде, **дорого по существу**: контракт описывает границу
   с MLRO, и сужение объявляет, что банк никогда не разрешит «с условиями». Это утверждение
   о надзоре, а не о типах.
2. **Расширить словарь надзора до шести.** Дороже в коде, но **убирает потерю смысла**: надзорный
   говорит то, что имеет в виду, и приведение становится тождественным.
3. **Оставить и записать.** Три исхода объявлены **про запас** — для производителя, которого пока
   нет. Честно **при условии**, что это записано; сегодня это нигде не записано, и шесть значений
   читаются как шесть возможных.

**Моя рекомендация — второй путь**, и довод не в стройности: сегодня надзорный, желающий сказать
«нужны сведения», обязан сказать «эскалация», и **получатель прочтёт это как отсутствие решения**.
Это не неудобство словаря, а **искажение записи о том, что человек решил**.

**Ратификация — ваша.** Замер, таблица и цена — здесь; проектировать вам ничего не нужно.

**Предел замера:** я считала по `libraries/` и `services/`, вне `imports/` и `dist`. Производитель
вне этих двух корней в счёт не вошёл бы.

## Пункт 8, окончательно: починка готова, и она упирается в один ваш вопрос

Сценарий порождения **починен** (ветвь `ssot-refinement-records`, ратифицированная карта **не
перепорождена**). Числа переноса:

```
              вход    выпущено генератором    не перенесено
до            330            319                   11
после         330            330                    0
```

**Терялось в трёх местах, а не в одном**, как я писала: при чтении записи, при укладке к родителю
и в цикле построения. И у каждой записи есть поле **`parent_position`** — родителя объявил
рецензент, а не вывел разбор строки; теперь он берётся из поля, а расхождение называется вслух.

### Ваш вопрос — ровно один

**Входит ли уточнение в знаменатель `N`?**

Порождённая карта: **324 → 335**. Пока каждому из одиннадцати не дана строка учёта, **три меры
краснеют**:

```
function-coverage   «функций вне обоих перечней»                 0 → 11
canonical-ssot      «лишняя запись, не объявленная ни в срезе»    0 → 11
canonical-ssot      «дрейф проекции: нет в активном реестре»      0 → 11
```

Это не поломка починки — это **честное следствие**: одиннадцать предметов появились, и каждый требует
собственного замера. **Корзиной «не смотрели» их не закрыть**: храповик стоит на нуле.

### Имя уточнений оставлено пустым НАМЕРЕННО, и это ваш же акт

Имена карты — глагольные обороты («восстановить доступ (recovery)»). `obligation_observable` —
**предложение о системе** («система относит расхождение к сошедшемуся или к разрыву по допуску»).
**Ни одно из одиннадцати формой имени не является.** Превращение требует морфологии, то есть
**сочинения текста, которого не рецензировал никто**.

Поэтому положено `name_gap` с этой причиной: **имя даёт владелец карты тем же актом, каким решает
судьбу знаменателя.** Владелец же взят родительским и назван полем `inherited_from` — пустое поле
подняло бы число организационных пробелов с 12 до 23, которых нет.

### Носители уточнений — в другом дереве, и это измерено

Все одиннадцать `entry_point` адресуют `bank-rooms/…` — каталог **репозитория архитектуры**, не
банковского дерева. Прямая проба банковского дерева: путей `bank-rooms/` — **ноль**.

В архитектурном дереве каждый адрес проверен по трём признакам (файл · строка в пределах · цитата
поведения **на названной строке**) — **11 из 11 да по всем трём**:

```
исполнимых   7      отсутствующих  3      только договор  1
```

И один из семи — `FN-RC-09/u2` — несёт вердикт **`contradicted`**: носитель есть и обязательству
**противоречит**. Это не пробел, а **известный дефект с адресом**.

**Чего отсюда не измерить:** есть ли у одиннадцати носитель в банковском дереве **под другим
адресом**. Носитель там устанавливается **объявлением человека**, и мера прямо отказывается выводить
его из имён файлов. Выводить машинно значило бы делать ровно то, что мера запрещает.

### Отдельно, и это тревожнее пункта

**Сценарий, порождающий принятую карту, не зовётся ни одним гейтом.** Проверено мною:

```
build-canonical-function-ssot в .gitlab-ci.yml:  0 вхождений
```

**Карта принята, а способ её получения ничем не проверяется.** Подключить сценарий значило бы
перепорождать ратифицированную карту в конвейере — это ваш акт, и потому здесь он только назван.

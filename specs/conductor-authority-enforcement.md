# Обязательный допуск действия в области полномочий дирижёра

> **S0-02 · РЕШЕНИЕ 4 · единый обязательный допуск, раздельные предметы проверки**
>
> Пункты S1-01…S1-03. Списки НЕ сводить механическим объединением. Разрешение MLRO не отменяет запрет модели. Добавить проверки: подмена выдающего · изменение содержимого · **самоназначение роли через тело запроса**.
>
> Основание: `DECISIONS-OPERATOR-ACCEPTED-2026-09-10.md`; учёт — `REGISTER-OPEN-ITEMS-2026-09-10.md`.


**Основание:** решение оператора 2026-09-10 по брифу
`BRIEF-CONDUCTOR-AUTHORITY-TWO-MODELS-2026-09-10.md` (`8690057e…`) и ответу консультантов
`RESPONSE-CONDUCTOR-AUTHORITY-TWO-MODELS-2026-09-10.md` (`648cc32b…`), включая дословное второе
мнение Codex.

## Цель

Один обязательный путь допуска действия, соединяющий **проверку полномочия** и **проверку
допустимости действия**. Две проверки сохраняют **разные обязанности**. Дублирующие правила
устраняются; **число файлов критерием дублирования не является**.

## Контекст: диагноз уточнён замером

Посылка брифа «две модели» **не подтвердилась**. Носителей полномочий пять; три проверки модели А
уже живут в шлюзе клиента; уникальный остаток А — один перечень `MODEL_MAY`. Доктрина «расширение
словаря не расширяет прав» уже закреплена пробой **для дирижёра**; недостаёт её пары **для
модели**.

**Три живые дыры, подтверждённые моим чтением на `e031b616`:**

1. **`allowed: [""]` проходит вход.** `authority-envelope.ts:134` требует массив строк,
   `:137` — непустую длину. Массив из одной пустой строки удовлетворяет обоим и разрешает любой
   шаг.
2. **`prohibited` не читается нигде.** Встречается дважды: объявлением типа (`:22`) и записью в
   настройке (`main.ts:199`). Принуждения нет.
3. **`grantedBy` проверяется ТОЛЬКО поиском имени** (`:167`, `issuers.trusted(...)`). Ни подписи,
   ни отпечатка, ни иной привязки выдающего к содержимому: предъявитель сам пишет имя доверенного.

**Отсюда поправка к моей же формулировке.** `verifyEnvelope` проверяет **предъявление** — форму,
заявленное имя выдавшего, область, отзыв. Он **не проверяет подлинность**. Где обеспечена связь
выдающего с содержимым — установить; если её нет, исправление входит в этот спринт.

**Живые дыры идут раньше мёртвого файла.**

## Область работ

### Входит
Обязательная точка допуска; достоверный контекст; проверка конкретного действия; перенос
действующих ограничений с единственным нормативным источником каждому; приоритет запрета;
отказ неизвестному; проверка до эффекта; проверяемый след; доктрина как правило, политика и
отрицательная проба; смысл `serverAuthority`; удаление `authority.ts` **после доказанного
замещения**.

### Не входит
Новый инфраструктурный контур удостоверения без доказанной недостаточности существующего.
**Расширение** таблицы допустимых действий MLRO и Director.

> **Поправка 2026-09-10 (решение оператора).** Прежняя редакция закрепляла за оператором «правку
> таблицы MLRO, её зеркала в research **и расширение её вызовов**». Последнее было ошибкой области:
> оно запрещало **подключить** уже существующее ограничение там, где оно не спрашивалось, и тем
> обращало незакрытую границу в закреплённую за оператором. **Подключение существующего ограничения
> не есть изменение распределения полномочий.** Разрешено прямо: подключить действующий рубеж MLRO
> как обязательный, исправить установление субъекта и проверку принадлежности роли, устранить обход
> проверки эмулированного держателя, обеспечить обязательное доказательство решения. Запрет
> сохраняется ровно на одно: **расширять перечни допустимых действий**.

## Функциональные требования

- **R1. Единый путь допуска.** Каждое действие в области полномочий дирижёра проходит обязательную
  проверку. HTTP-вход, внутренний вызов, повтор и восстановление задачи обхода не дают.
- **R2. Достоверный контекст.** Исполнитель, выдающий, область, требуемое подтверждение и класс
  данных определяются доверенными механизмами. Клиент или модель не получают прав, передав
  `actorIsModel=false`, более высокий `assurance` либо изменённый `allowed`.
- **R3. Проверка конкретного действия.** Проверенный конверт разрешает **именно запрошенное**
  действие над соответствующим объектом в нужном tenant/work scope. **Непустой `allowed`
  недостаточен.**
- **R4. Сохранение ограничений.** `MODEL_MAY`, `NEVER`, требования `assurance`, подтверждения вне
  модели и ограничения данных перенесены и сопоставлены с существующими механизмами; **одному
  правилу — один нормативный источник**.
- **R5. Приоритет запрета.** Полномочие от доверенного выдающего не разрешает пересечь независимую
  границу MLRO и не разрешает запрещённое действие. **Повышение `assurance` запрета не отменяет.**
- **R6. Неизвестное не разрешается.** Неизвестный глагол, действие, актор или отсутствующая
  политика дают отказ. **Новое слово в словаре прав не расширяет.**
- **R7. Проверка до эффекта.** Допуск проверяется **перед** защищаемым действием. При очереди,
  повторе и восстановлении учитывается **актуальный** отзыв; принятый ранее запрос бессрочным
  разрешением не становится.
- **R8. Проверяемый след.** Решение оставляет связанное доказательство: актор, действие, область,
  идентификатор полномочия, версия политики, исход и причина отказа — **без секретов и клиентских
  данных**.

**`serverAuthority`:** объявлено в старой модели и её `assertAuthorised` не проверяется. Определить
смысл по источнику требований и реализовать принуждение либо **доказанное** замещение.
**Механический перенос поля закрытием требования не считается.**

## Устройство: разделение обязанностей сохраняется

```
доверенный контекст и проверенное полномочие
            ↓
допустимость конкретного действия для данного актора
            ↓
существующее локальное принуждение
            ↓
эффект и связанное доказательство
```

Проверка конверта и предметная политика могут остаться **отдельными модулями**, вызываемыми
обязательной точкой допуска. Предметная политика **не привязывается к HTTP**: внутреннее
исполнение соблюдает её тоже.

Различие сохраняется: **модель готовит предложение, служба исполняет разрешённое действие**.
Запрет исполнения моделью не превращается в запрет автоматизации; служба не служит способом скрыть
исполнение от имени модели.

## Доктрина — тремя способами одновременно

«Расширение словаря не расширяет полномочий» живёт как **правило с происхождением**, как
**исполняемая политика** и как **отрицательная проба**.

**Уточнение оператора:** новый глагол требует **явной классификации**, а не обязательного
разрешения. **Запись `DENY` есть полноценное решение.** Добавлять глагол в `MODEL_MAY` ради
зелёной пробы запрещено.

Проверка полноты обнаруживает новый глагол **без классификации**; во время исполнения неизвестное
действие отвергается **независимо** от результата такой проверки.

## Критерии приёмки

- **A1.** Корректное разрешённое действие проходит и оставляет ожидаемый результат.
- **A2.** Корректный конверт с запрещённым действием — отказ **без защищаемого побочного эффекта**.
- **A3.** Подмена `grantedBy`, `allowed`, актора или `assurance` полномочия не создаёт.
- **A4.** Чужая либо недостающая обязательная область отвергается.
- **A5.** Отозванное полномочие не действует, **включая отзыв после постановки в очередь**.
- **A6.** Модель не получает прав через `observe`, `request_consultation`, новый глагол или подмену
  актора службой.
- **A7.** Ограничения MLRO сохраняются **при любом предъявленном `assurance`**.
- **A8.** Требуемое подтверждение вне модели не заменяется полем из недоверенного запроса.
- **A9.** Внутренний вызов и восстановление задачи проверку не обходят.
- **A10.** Намеренное отключение предметной проверки делает интеграционную пробу **красной**.

**Пять существующих причин отказа конверта остаются различимыми.** Новые причины предметного отказа
доходят до вызывающего и журнала и **не превращаются в общий «неуспех»**.

## Удаление `authority.ts` — только после доказанного замещения

До удаления составить соответствие:

```
старое правило → действующий источник → новый исполнитель → проверяющий сценарий
```

Проверить импорты, потребителей, экспортируемые типы, документацию, ограничения происхождения.
**Если модуль остаётся единственным источником предметной политики и подключён к живому пути —
удалять его ради сведения файлов не требуется.**

## Проверка

Каждый `R*` и каждый `A*` проверяется **отдельно**, с указанием SHA, команды, результата и
наблюдаемого поведения. Канарейка обязательна там, где утверждается принуждение: снятие меры
обязано ронять её пробу при неизменном общем числе проб.

## Определение готовности

PASS по каждому `R*` и `A*` с доказательством; полный локальный набор гейтов; CI зелёный на точном
SHA, прочитанный гейтом измерения. Любой FAIL, PARTIAL или BLOCKED означает **NOT CLEAN** для этого
спринта.

Влитая ветка и post-merge сюда **не входят** — они проверяются интеграционным циклом после CLEAN.
**Закрытие этого дефекта закрытием 324 функций не является.**

---

# Дополнение 2026-09-10: недоверенное заявление как основание полномочий

**Основание:** решение оператора 2026-09-10 после независимой проверки коммита `e7edacc0`.
R1–R8 и A1–A10 выше **сохраняются без изменений**; ниже — то, что ими не покрывалось.

## Уточнение объёма доказанного исправления

Формулировка коммита «три живые дыры закрыты» **шире доказанного**. По прямой пробе
(`evidence/probes/central-authority-substitution-probe-2026-09-10.spec.ts`, снимок `e7edacc0`):

| Дыра | Заявлено | Измерено |
|---|---|---|
| 1. `allowed: [""]` открывал всё | закрыта | **закрыта** — `patternBody` отвергает пустой образец; контрольная проба подтверждает |
| 2. `prohibited` не читался | закрыта | **читается — но у того, кого ограничивает**: `prohibited: []` от предъявителя стирает договорный запрет, `permitted: true` |
| 3. `grantedBy` — только имя | закрыта | **закреплено ИМЯ конверта, не содержимое**: подмена `allowed` у выданного конверта даёт `verify: true, admit: true` |

**Общее у второй и третьей:** запись о выдаче связывает `envelopeId → grantedBy` и не связывает
`allowed`, `prohibited`, `scope`. Класс тот же, что закрывали: **имя не есть доказательство** —
поднятое на уровень выше, с имени выдающего на имя конверта.

## Предмет дополнения одной фразой

**Вызывающий не может назначить себе личность, роль, содержимое полномочия или право принять
решение. Отсутствие обязательного механизма проверки не разрешает действие.**

## Функциональные требования дополнения

- **R9. Предъявленное отделено от установленного.** `decidedBy`, `roles`, `grantedBy` и подобные
  поля запроса **не устанавливают** личность и права. Субъект определяется доверенным механизмом
  входа; роли выводятся из реестра, связанного с этим субъектом. Поле клиента есть **проверяемое
  заявление**, а не доказательство.
- **R10. Роль не выводится из звучания имени.** Удаляется определение роли по наличию `/MLRO/`,
  по словам `director`, `engine`, `conductor` и по любому иному сходству строки с должностью.
  **Неизвестный субъект не становится `human` по умолчанию.** В песочнице используются явно
  зарегистрированные личности двойников: знание строки `sim://…` не даёт права действовать от
  имени двойника.
- **R11. Выданное полномочие закреплено за содержимым и субъектом.** Записи `envelopeId → grantedBy`
  недостаточно. Доверенная запись выдачи определяет получателя, выдающего, разрешённые и
  запрещённые действия, область tenant/work/resource, условия действия и отзыва. Исполнение
  использует **эту запись** либо доказанно связанное с ней предъявление. Изменение клиентом
  `allowed`, **удаление или опустошение** `prohibited`, подмена `scope` выданных прав не меняют.
  Хеш, вычисленный самим клиентом, доказательством выдачи не является. Подпись или mTLS **не
  предписываются**: сначала используется подходящий существующий механизм. **Критерий —
  невозможность подмены, а не наличие криптографического термина.**
- **R12. Обход проверки двойника устранён.** В `assertHolders` эмулированное назначение уходит в
  `continue` **до** сравнения держателя с `decidedBy`, и потому посторонний проходит как MLRO.
  Эмуляция снимает требование **живого назначенца**, но не проверку того, **кто вправе
  представлять двойника**. Пробы, закрепляющие обратное (`seats.spec.ts` — «кто-угодно»,
  «неважно»), исправляются вместе с механизмом.
- **R13. Обязательное — обязательно.** Отсутствующий `mlroBoundary` **блокирует** операцию.
  Допустим отказ запуска возможности либо явный отказ маршрута; **продолжение без проверки
  недопустимо**. Необязательный страж есть страж, которого может не быть.
- **R14. Решение не остаётся без доказательства.** Если независимый след обязателен, то
  необязательная запись (`mlroJournal?.append`) **после** изменения дела оставляет принятое решение
  недоказуемым. Изменение и обязательный след согласуются существующим механизмом хранения.
  **Отказ записи не оставляет незамеченным успешно принятое решение.**

## Порядок проверки защищённого решения — до изменения состояния

```
кто обращается → какая роль ДЕЙСТВИТЕЛЬНО принадлежит этому субъекту
→ применима ли роль к делу и запрошенному действию
→ разрешает ли действующий рубеж независимой линии
→ можно ли сохранить требуемое доказательство результата
→ и только затем изменение состояния
```

## Перечень маршрутов — конечный, а не аудитный трек

Тот же дефект ищется по маршрутам: принятие решений; выдача и отзыв полномочий; остановка и
возобновление; восстановление и исполнение задач. Для каждого фиксируется строка:

```
действие → доверенный субъект → полномочие → обязательная проверка → изменение состояния → доказательство
```

**Мерило дефекта — последствие отсутствия, а не форма записи.** Не всякий `if (parts.x)` или `?.`
есть дефект:

- отсутствие обязательной защиты **с продолжением действия** — дефект;
- отсутствие возможности **с отказом операции** — допустимое поведение;
- необязательная вспомогательная функция оценивается по своему контракту.

**Поиск заканчивается перечнем проверенных маршрутов и исправлениями.**

## Критерии приёмки дополнения

- **A11.** Посторонний присылает `decidedBy` со строкой `/MLRO/` — решение **не меняется**.
- **A12.** Он присылает **точное имя зарегистрированного двойника** и `roles: ["MLRO"]` — подмена
  также отвергается.
- **A13.** Разрешённый субъект принимает допустимое решение и оставляет **полный след**.
- **A14.** Director не получает прав MLRO ни конвертом, ни высоким `assurance`, ни подменой роли.
- **A15.** Отсутствие рубежа **блокирует операцию до изменения дела**.
- **A16.** Изменение прав или области ранее выданного конверта отвергается.
- **A17.** Отозванное полномочие не действует при повторе и при восстановлении задачи.
- **A18.** Внутренний вызов очереди проверку субъекта не обходит.
- **A19.** Сбой обязательного журнала не оставляет решение без восстанавливаемого доказательства.
- **A20.** Намеренное отключение **каждой** исправленной защиты делает соответствующую
  интеграционную пробу **красной**.

**Проверяется не код ответа.** Сравнивается состояние дела, очереди, полномочия и журналов **до и
после**. При отказе допустима запись об отказе; защищённое действие выполняться не должно.

## Состояние спринта

**NOT CLEAN** — и остаётся им, пока не проверены все перечисленные маршруты. Проверенные пробой
дефекты A3, A2 и R12 закрытыми не считаются до предъявления пробы, которая их роняет.

Отчёт различает пять состояний, и они не сливаются: **дефект найден** · **исправление написано** ·
**проверено на SHA** · **интегрировано** · **проверено после интеграции**. Передача задания
исполнителю выполнением моей части не является.

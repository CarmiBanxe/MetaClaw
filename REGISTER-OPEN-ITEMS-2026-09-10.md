# Реестр незакрытых пунктов

**Основание:** `DECISIONS-OPERATOR-ACCEPTED-2026-09-10.md`. **Снимок отсчёта:** `b87a9041`.
**Ведёт:** центральный терминал. **Исполняет:** Фабрика.

**Правило записи.** Пункт закрывается не коммитом и не спецификацией, а **проверяемым
результатом**, названным в его строке. Покрытие пересчитывается по этим ID, **без двойного
учёта**: функция, засчитанная за одним пунктом, за другим не засчитывается.

**Владельцы:** `Ф` — Фабрика (реализация) · `Ц` — центральный терминал (решение, спецификация,
независимая проверка) · `О` — оператор (боевой контур) · `И` — инфраструктура.

---

## Спринт 0 — основания и замеры на одном снимке

| ID | предмет | вл. | зависит | проверяемый результат | следующее действие |
|---|---|---|---|---|---|
| S0-01 | все замеры брифа пересняты на одном снимке | Ц | — | таблица «предмет → число → команда» на одном SHA, расхождения с брифом названы поимённо | снять и записать |
| S0-02 | спецификации приведены к решениям | Ц | S0-01 | каждая из 15 несёт ссылку на решение либо помечена «решением не затронута» | обход по одной |
| S0-03 | инфраструктурные зависимости названы | Ц·И | S0-01 | перечень: вторая модель · runner с изоляцией · хранилище ключей — каждая с признаком проверки наличия | замер доступного, а не предположение |
| S0-04 | боевые решения выделены отдельно | Ц·О | — | перечень условий боевого запуска, не блокирующих песочницу | свести из пунктов 2·8·10 |

## Спринт 1 — полномочия, контекст, выход, прерывание, handoff

| ID | предмет | вл. | зависит | проверяемый результат | следующее действие |
|---|---|---|---|---|---|
| ~~S1-01~~ | ~~единый обязательный допуск~~ | — | — | **ЗАКРЫТ 2026-09-11 замером**: четыре слоя исполнены, три канарейки различают. Обход закрыт: «необъявленное полномочие есть отказ» (`code-effect.ts:189`), проверка времени исполнения заведена потому, что типа оказалось мало | — |
| S1-02 | отображение разных словарей действий | Ф | S1-01 | неизвестное соответствие даёт **отказ**, а не проход; проба на неизвестную пару | — |
| ~~S1-03~~ | ~~подмена, содержимое, самоназначение роли~~ | — | — | **ЗАКРЫТ 2026-09-11**: обход всех шести дверей; самоназначение закрыто, канарейка `roles.slice(0,1)` роняет ровно пробу «своя + приписанная». Шестая дверь несёт ту же дыру, но отзыв ОДНОСТОРОНЕН — ошибается в сторону покоя | — |
| S1-04 | контекстный набор инструментов на доверенной стороне | Ф | — | **ключевая проба оператора**: до работы `предложен card.freeze, вызван payment.send → ПРОШЛО`; после — `TOOL_OUTSIDE_CONTEXT_SET` | **спецификация написана** `specs/offered-tool-context.md`; дефект показан прогоном |
| ~~S1-05~~ | ~~истечение разрешения~~ | — | — | **ЗАКРЫТ 2026-09-11**: канарейка `>=`→`>` против полного набора шлюза даёт `1 failed of 148`. Дерево ловит миллисекунду само | — |
| S1-06 | политика выхода на доверенной стороне | Ф | — | отправитель не разрешает свой пакет; перечень разрешённого приходит извне пакета | спецификация `egress-allowlist-not-from-sender` уже написана |
| S1-07 | обходы выхода: переименование, вложенность, сочетания | Ф | S1-06 | три пробы обхода, каждая отвергается | — |
| S1-08 | удостоверение вызывающего у рычага прерывания | Ф | — | посторонний со строкой поднимает И СНИМАЕТ остановку (показано прогоном, 200/200) | **спецификация написана**; образец переносится из двери решений (`DECIDER_CLAIM_MISMATCH`) |
| S1-09 | handoff MLRO в исполняемый путь песочницы | Ф | S0-02 | цепь двери измерена: **14 предусловий**, контракта в ней нет. `assertRequestWellFormed`/`assertDecisionAcceptable` — 0 производственных вызывающих | подключить контракт в конце цепи, ДО изменения состояния |
| ~~S1-10~~ | ~~согласованность решения и следа~~ | — | — | **ЗАКРЫТ 2026-09-11**: намерение пишется ДО решения, исход ПОСЛЕ; при отказе второй записи дверь НАЗЫВАЕТ оба факта — `DECISION_WITHOUT_EVIDENCE` + `decided: true`. Две канарейки различают | — |
| ~~S1-11~~ | ~~заголовки библиотеки~~ | — | — | **СНЯТ 2026-09-11: дефекта нет.** Заявление стоит один раз и сказано о ФАЙЛЕ («ни одна функция ЗДЕСЬ»); все три его части проверены — сеть 0, запись 0, вызывающих 0 и 0. Поручение возникло из моей неточности | — |

## Спринт 2 — карантин, долговечность, KYC, корень Director

| ID | предмет | вл. | зависит | проверяемый результат | следующее действие |
|---|---|---|---|---|---|
| S2-01 | порядок приёмки при отсутствующем донорском наборе | Ц | S0-02 | шесть условий маршрута записаны в реестр карантина как отдельный путь | спецификация |
| S2-02 | пометка «тест создан сейчас» | Ф | S2-01 | новые пробы несут признак; **исторический паритет ими не объявляется** | — |
| S2-03 | правка целевого кода под карантином разрешена | Ц | S2-01 | донорский снимок неизменен, различия зарегистрированы | внести в реестр |
| S2-04 | **16 обязательств долговечности** | Ф | S2-03 | по каждому: проба перезапуска · сбоя записи · повторного запроса. **Наличие в памяти доказательством не считается** | приоритет |
| S2-05 | перенос `KYCProviderPort` в библиотеку | Ф | S0-02 | экспорты, разрешение импортов, `no-service-to-service` зелен; поведение не изменилось | — |
| S2-06 | восемь функций KYC | Ф | S2-05 | закрываются по **фактической интеграции и проверкам**, а не по появлению библиотеки | — |
| S2-07 | рабочий путь корня Director в CI | Ф·И | S0-03 | сборка · открытие хранилищ · сценарий · перезапуск · чтение сохранённого | нужен runner с изоляцией |
| S2-08 | воспроизведение конфликта журнала и следа | Ф | S2-07 | прогон воспроизводит исходный дефект | — |
| S2-09 | отказной сценарий сохранён отдельно | Ф | S2-07 | оба сценария в конвейере, ограничения изоляции не отключены | — |

## Спринт 3 — независимый проверяющий и цепи

| ID | предмет | вл. | зависит | проверяемый результат | следующее действие |
|---|---|---|---|---|---|
| S3-01 | доступность второй модели | Ц·И | S0-03 | **замер**, а не предположение: что есть в контуре помимо входа `main.ts` | сделать первым |
| S3-02 | `ReviewPort` производственный | Ф | S1-09 | личность проверяющего берётся у выдавшего | **строить сейчас** |
| S3-03 | `BrokerPort` производственный | Ф | S1-06 | квитанция модели связана с пакетом | **строить сейчас** |
| S3-04 | `OversightDecisionPort` производственный | Ф | S1-09 | квитанция надзора связана с делом | **строить сейчас** |
| S3-05 | композиция `LocalEnforcement` | Ф | S3-02·03·04 | собрана в живом процессе | **строить сейчас** |
| S3-06 | **исполнение только по полному комплекту** | Ф | S3-05 | проба: три квитанции из четырёх → отказ; четыре несвязанных → отказ | граница проходит здесь |
| S3-07 | обязательность review и oversight — серверной политикой | Ф | **делать ДО S3-05** | **дефект показан прогоном:** три поля запроса в `false` → `enforced: true` с нулём квитанций, включая денежное действие. Сегодня это ЗАПАДНЯ (композиции нет); сборка превращает её в течь | показ дефекта сохранён |
| S3-08 | повтор не обходит проверку чужим ключом | Ф | **делать ДО S3-05** | **дефект показан прогоном:** чужой `actionKey` проводит ДЕНЕЖНОЕ действие без квитанций и ЗАПРЕЩЁННОЕ `aml.override`. `ALREADY_ENFORCED_DIFFERENTLY` объявлен и не выбрасывается ни одной строкой | показ дефекта сохранён |
| S3-09 | цепи `AML` · `CLIENT_REQUEST` · `PAYMENT` | Ф | S3-05 | звено «локальное принуждение» перестаёт быть `НЕ ЗОВЁТСЯ` | закрывается последним |

## Вне спринтов — учёт и гигиена

| ID | предмет | вл. | проверяемый результат |
|---|---|---|---|
| X-01 | аттестация слияний: отменённое требование убрать из действующей оценки | Ц | мера не отказывает на отменённом основании; история сохранена; четыре слияния получили **применимое** основание |
| X-02 | запись факта слияния | Ф | SHA · источник события · связанные CI и review, если существуют. **Нового барьера не вводится** |
| X-03 | ADR по I-04 | Ц·О | каждое значение прослежено до действия; проверено ниже/ровно/выше; двухступенчатость **не объявлена** нормой без доказательства |
| X-04 | тестовый профиль порогов для песочницы | Ф | профиль помечен явно; боевые пороги не затронуты |
| X-05 | боевое назначение держателя рычага | О | механизм назначения, отзыва, аудита готов; **назначение человека — условие боевого запуска** |
| X-06 | пересчёт покрытия по ID без двойного учёта | Ц | таблица «ID → функции» без пересечений; итог сверен с гейтом |

---

## Что уже сделано и в реестр не входит

18 доказанных проб с канарейками · 15 спецификаций · два прибора (`gate-sweep.py`,
`type-vs-probe.py`) · вилка долга непроверенных отказов `37…77` с калибровкой.

**Это не закрытые цепи и в покрытие не идут.** Числятся как основание для пунктов выше.

---

# СОСТОЯНИЕ НА 2026-09-11, поздний такт

**Прежние строки выше не переписаны** — они показывают, с чего начинали. Здесь итог.

## Закрыто ЗАМЕРОМ — пятнадцать пунктов

У каждого канарейка, подсадка либо прямая сверка; ни один не закрыт чтением.

| ID | чем закрыт |
|---|---|
| **S0-01** | 18 величин на одном снимке, расхождений с брифом ноль |
| **S0-02** | 15 спецификаций несут ссылку на решение либо пометку |
| **S0-03** | моделей три, пригодных две, **иного семейства ноль**; `llama3.3:70b`, `deepseek-r1:70b` объявлены и не скачаны |
| **S0-04** | реестр боевых условий **уже есть** — 29 флагов; ни один не блокирует песочницу |
| **S1-01** | четыре слоя допуска исполнены, три канарейки; обход закрыт «необъявленное полномочие есть отказ» |
| **S1-03** | обход шести дверей; самоназначение роли закрыто, канарейка `roles.slice(0,1)` |
| **S1-05** | канарейка `>=`→`>` против полного набора: `1 failed of 148` |
| **S1-10** | три состояния — три ответа; при отказе записи дверь **называет оба факта** |
| **S1-11** | **снят: дефекта нет** — заявление о ФАЙЛЕ, все три части верны |
| **S2-01** | **посылка о стене опровергнута**: наборы есть у 85 из 99, два прогнаны (98 и 101 passed) |
> ## ⚠ ОШИБКА 29-я, НАЙДЕНА ИСПОЛНЯЮЩЕЙ ЛИНИЕЙ 2026-09-11: «13 ИЗ 13» НЕ ОЗНАЧАЛО НИЧЕГО
>
> **Прогон, который я объявила доказательством исполнимости рабочего пути, был мёртв.**
>
> Линия, посланная внести его в конвейер, прогнала корень **прежде** чем заводить задание — и
> получила **тринадцать сценариев, где КАЖДЫЙ отвечал одним и тем же `403 AUTHORITY_NOT_ISSUED`**.
> Обращение отвергалось **на первом рубеже полномочий**: до надзора, до цели, до модели, до записи,
> до приёмки.
>
> **Чем было моё число.** Оснастка печатает `console.error(«сценариев: ${результат.сценарии.length}»)`
> — **длину массива записей**. Галочка ставится за **запись сценария, а не за его исход**. Я взяла
> длину массива за число пройденных сценариев.
>
> **Почему прогон умер.** `AUTHORITY_NOT_ISSUED` и реестр выданных конвертов заведены **2026-09-10**,
> на три дня позже оснастки; оснастка не подаёт `DIRECTOR_ISSUED_ENVELOPES` — проверено, вхождений
> **ноль**. **Четыре дня она печатала прежнее число, не доходя до предмета.**
>
> **Чем это было раньше** — по свидетельству `48d1c872` от 2026-09-07: `200 ACCEPTED`, клетка
> ENFORCED, файл создан, записей 107, после перезапуска `отзыв_пережил: true`.
>
> **Внесённая в обязательную проверку как есть, она закрепила бы ОТКАЗНОЙ путь под именем рабочего**
> — ровно то, о чём предупреждает решение 5. Линия починила оснастку прежде, чем заводить задание.
>
> **Разряд ошибки — мой собственный, трижды названный сегодня же:** число, которое считает **записи,
> а не исходы**. Так же я считала «сценариев 151» по длине строки прозы и «мер без подсадок 25» по
> узкому образцу. Здесь цена была выше: число ушло оператору как доказательство.
>
> **Действующее:** рабочий путь исполним **после починки оснастки**, и это показано линией —
> `EXIT=0`, 14 сценариев, исходы **различны** и проверяются поимённо, гейт ловит **вырождение
> исходов в один код** — тот самый способ, каким оснастка умерла молча.

| **S2-07** | рабочий путь исполним: **13 из 13**; задача инфраструктуры названа |
| **S2-08** | подсадка: единичная проба ловит, гейт и живой прогон — нет |
| **S2-09** | отказной сценарий — отдельное задание конвейера |
| **S3-01** | измерено вместе с S0-03 |
| **X-01** | **неверен был мой обход**; после исключения снятых мер — `отказало 0` |
| **X-03** | значения прослежены до действий; граница закреплена деревом (`1 failed of 199`) |
| **X-06** | 277 разнесены по пунктам, **пересечений ноль**; потолок 49 → **57** |
| **X-02** | журнал есть запись **пути**, не факта: все **5 человеческих** слияний без записи, автоматных 29 из 144. Мера `tools/merge-fact-record.py` выводит запись из git, расхождение 0 по построению, две различающие канарейки |
| **X-03** | **ADR написан.** Прибор был уже предмета: дерево несёт реестр 6 величин / 30 домов / 12 исключений. Обе постоянные книги не значатся ни домом, ни исключением. Признак одобрения — поле вызывающего, показано исполнением; **это паритет с донором**, не дефект переноса |
| **X-04** | **спецификация написана** `specs/sandbox-threshold-profile.md`: режим — с доверенной стороны; тестовый порог только НИЖЕ боевого; две обязательные канарейки |
| **S2-04** | **1 из 16 починено** (дирижёр: порт доводом сборки, перезапуск зелен), 15 остаётся; четыре ошибки моего прибора названы |
| **S2-05/06** | порт **без единого внешнего потребителя**; восемь функций объявлены и **не начаты** — ни носителя, ни покрытия |
| **сверка решений** | десять решений сличены **дословно** с 28 спецификациями: пять настоящих пробелов закрыто, четыре ложные тревоги — узкие приборы 6–9 |
| **прогон проб** | **31 файл · 142 пробы · красных 0** в снимке `b87a9041`; единственная красная (`s1-09`) доведена до названного предмета — запрещённое поле проходит дверь, **решение принимается и состояние меняется** |
| **приборы дерева** | гейт покрытия и гейт цепей проверены подсадкой; учёт **замкнут** (47+277=324), подделка требует ДВУХ правок; знаменатель 324 подтверждён — принятая карта есть точное подмножество 335 |
| **подсадки мер** | 71 из 78 несут подсадки; **принуждающих без них НОЛЬ**; три самопроверки не зовутся конвейером — спецификация `orphan-selftests-into-pipeline` |
| **мои числа отсутствия** | семь утверждений перемерены широким образцом: **шесть держатся, одно опровергнуто** (носитель восьми KYC). Правило: первое число отсутствия есть ВЕРХНЯЯ ГРАНИЦА |
| **коды против имён** | 90 из 91 кода отказа уникальны; имён классов сталкивается **73**. Утверждение по коду различает, по имени класса — нет |
| **обход проб** | 24 из 31 несут канарейку с числами; три находки, включая `expect(true).toBe(true)` в моей же пробе и чтение живого дерева вместо снимка |

## Дефект показан прогоном, спецификация написана — восемь пунктов

`S1-02` · `S1-04` · `S1-06` · `S1-07` · `S1-08` · `S3-06` · `S3-07` · `S3-08`

**Три из них — западни сборки** (`S3-06/07/08`) и переставлены **до** `S3-05`: сборка вносит их
одновременно.

## Остаётся работой Фабрики

| ID | предмет |
|---|---|
| S1-09 | подключить контракт handoff в конец цепи из 14 предусловий |
| S2-02…S2-05 | пометка проб · реестр различий · 16 обязательств долговечности · перенос KYC |
| S2-06 | восемь функций KYC — **по фактической интеграции**, не по появлению библиотеки |
| S3-02…S3-05 | три порта и композиция — **после** трёх западней |
| S3-09 | три цепи: звено перестаёт быть `НЕ ЗОВЁТСЯ` |

## Остаётся за оператором

`X-05` боевое назначение держателя · **225 функций за условием 2** — названным человеком-выпускающим ·
**четыре решения ADR по I-04**: разряд двух постоянных книги · связь пометки и удержания ·
вправе ли признак одобрения оставаться булевым · применимость I-04 к фиатной книге (I-05).

`X-02`, `X-03`, `X-04` подготовлены и переданы: мера, ADR и спецификация лежат в дереве.

## Числа, которыми это меряется

```
покрытие                47 / 324      достижимо от службы  38 / 324
потолок без оператора   57 / 324      (было 49 — решение 3 добавило восемь KYC)
обход мер               69 = прошло 67 + ОТКАЗАЛО 0 + неверный вызов 2
донорские наборы        85 из 99 есть · 2 прогнаны зелёными
```


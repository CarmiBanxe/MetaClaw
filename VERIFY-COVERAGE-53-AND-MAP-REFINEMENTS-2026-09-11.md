# `52/324` → `53/324` · беззащитная проба оказалась ПУСТОЙ · и карта закрывает пять функций сама

**Ветвь** `map-move-two`, коммиты `67ee5cfe` и `89069121`. **Проверял:** центральный терминал.

## Проверено моими прогонами

```
function-coverage:  PASS  FUNCTION_COVERAGE=53/324
full-chain:         PASS  LINKS_CLOSED=1081 (планка 1081)  FULL_CHAINS=0/324
unfailable-refusal: 1863 файла · проб отказа 156 = защищённых 156 + беззащитных 0
учёт: covered 53 + blocked 271 = 324 · пересечение 0
```

## Беззащитная проба была НЕ ПРОСТО беззащитна — она была ПУСТА

Мера называла форму: утверждения только в `catch`. **Страж вскрыл глубже.**

> Ломкий журнал отказывает на записи **№ 1**, а она **уже израсходована** стоящим выше
> утверждением. Вызов внутри `try` **не бросал вовсе**, `catch` был **мёртвым кодом**, и оба
> `expect` **не исполнялись никогда**.

**Лечено усилением:** свежий владелец со свежим ломким носителем — утверждения о тексте отказа
наконец проверяются.

**Выбор канарейки сделан с доводом, и довод точен:**

> «Ослепи я сам бросок — покраснела бы и строка выше, **и различения бы не вышло**.»

Ослеплён **текст отказа**, а не бросок. Итог различает:

| прогон | вердикт |
|---|---|
| ослеплённый код + **починенная** проба | 1 красная |
| ослеплённый код + **исходной** пробой | **199 зелёных — проба была слепа** |

## Образец стража взят У САМОЙ МЕРЫ, и подмена отвергнута по существу

`expect.fail` не взят, потому что это **Vitest**, а здесь **Jest**: взят `throw` — «тот самый оборот,
каким живут остальные 155 проб дерева». **Образец из меры, а не из памяти о похожем.**

## Карта закрывает пять функций САМА — и это восьмой пункт оператору

**`FN-AU-03` не переведена — и не за недоделанность.** Носитель есть, все четыре якоря дают целое
совпадение **по вызову её же `anchored()`**, граница выражена значением, набор `auth` — **372 пробы,
17 наборов, КОД=0**.

**Мешает дефект КАРТЫ.** Проверил сам прямым перебором:

```
функций с НЕРАЗРЕШАЮЩИМИСЯ уточнениями: 5
   FN-AU-03  FN-AU-08  FN-PM-15  FN-RC-05  FN-RC-09     — одиннадцать уточнений, ни одно не
                                                          существует как функция карты
```

> **Пять функций закрыты для объявления не работой, а картой:** она числит за ними уточнения,
> которых **в ней самой нет**.

**Линия не дописала определения, и это верно:** «дописать значило бы **решить за владельца карты,
что они означают**». Функция названа поимённо в `rejected_this_pass`.

## Отсутствие живого пути записано, а не умолчано

У `FN-PL-16` нет ни `wired_at`, ни `consumer_at`, ни `seam_at`. Заведено поле `living_path_absent`
со **сплошным** замером: **28 вхождений вне носителя — все 28 в одном наборе проб**; единственное
упоминание вне — переэкспорт бочки.

**Живого пути нет — так и написано.** Поле, которого нет, сказало бы меньше, чем поле, называющее
отсутствие.

## Линия НЕ воспользовалась известным пределом меры — и сказала об этом

Дельта перехода **1**, а имён функций в новом переходе **два**: мера **считает** имена и **не
сверяет** их с `covered` — зачла бы оба.

> Линия назвала предел вторично **в самом отчёте** и сказала прямо, что второе имя —
> **не переведённое**, вместо того чтобы воспользоваться тем, что **мера не спросит**.

## Звенья: три по одному, вниз ничего

`1 источник 17→18 · 8 носитель 52→53 · 9 контроль 52→53`. Долю звена 1 проверено **прямым
опросом**, а не вычитанием. Прочие семь — без движения.
